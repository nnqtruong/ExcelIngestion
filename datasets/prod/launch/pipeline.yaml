# Dataset: launch (prod)
# Environment path: datasets/prod/launch — run with: --env prod --dataset launch
name: launch
environment: prod
description: "Launch tracking data - employee onboarding by track, division, and team lead"

steps:
  - 01_convert
  - 02_normalize_schema
  - 03_add_missing_columns
  - 04_clean_errors
  - 06_combine_datasets
  - 08_validate
  - 09_export_sqlite

sqlite:
  database: warehouse.db
  table: launch
  indexes:
    - track
    - division
    - subdivision
    - team_lead
