# Dataset: workers (prod)
# Environment path: datasets/prod/workers — run with: --env prod --dataset workers
name: workers
environment: prod
description: "Workday HR worker export - comprehensive employee dimension"

steps:
  - 01_convert
  - 02_normalize_schema
  - 04_clean_errors
  - 05_normalize_values
  - 08_validate
  - 09_export_sqlite

sqlite:
  database: warehouse.db
  table: workers
  indexes:
    - employee_id
    - current_status
    - work_email
